pipeline {
    agent any
    environment {
        DOCKERHUB_CREDENTIALS = credentials('docker_hub')
    }
    stages {
        stage ("Build") {
            steps {
                echo 'Starting Build ...'
                }
        }
        stage ("Test") {
            steps {
                echo 'Starting Test ...'
            }
        }
        stage ("Deploy") {
            steps {
                echo 'Starting Deploy ...'
            }
        }
        stage ("Build docker image"){
            steps {
                 sh 'docker build -t medch1/simpleapp .'
            }
        }
        stage ("Push Docker image"){
            steps {
				sh 'echo $DOCKERHUB_CREDENTIALS_PSW | docker login -u $DOCKERHUB_CREDENTIALS_USR --password-stdin'
                sh 'docker push medch1/simpleapp '
            }
        }
        stage ("Notify Discord"){
            steps  {
                //sh 'snyk container test medch1/simpleapp --file=Dockerfile'
                discordSend description: env.JOB_NAME +" \n Build: " +env.BUILD_NUMBER +"" , footer: '', image: '', link: env.BUILD_URL, result: currentBuild.currentResult, scmWebUrl: '', thumbnail: '', title: 'Jenkins Job Update ', webhookURL: 'https://discord.com/api/webhooks/1067038928391110707/JjT7HUtmLFT2aaHLj65ZUR2FjkKVj3xeZWe2eEzYEVEqFEeL4ebPGlzbQDg2NNGMifep'

            }
        }
    }
}




//checkout([$class: 'GitSCM', branches: [[name: '*/main']], extensions: [], userRemoteConfigs: [[credentialsId: 'gitHub', url: 'https://github.com/medch1/docker-mongo.git']]])

//snykSecurity severity: 'critical', snykInstallation: 'Snyk', snykTokenId: 'snyk_cred'
